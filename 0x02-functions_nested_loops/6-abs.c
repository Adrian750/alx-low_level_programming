#include "main.h"

/**
 * _abs - function to print absolute value of integer
 * @n: value to be checked
 * Return: Returns value of 0 on success
 **/

int _abs(int n)
{
	if (n >= 0)
		return (t);
	else
		return (-t);

}
